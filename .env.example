# Development Environment Configuration
# Copy this file to .env and fill in your values

# Application Environment
NODE_ENV=development

# Server Configuration
PORT=5000

# Database (Optional in development - will use in-memory session store if not provided)
# DATABASE_URL=postgresql://user:password@host:port/database

# Authentication (Optional in development - auth features will be disabled if not provided)
# REPLIT_DOMAINS=your-replit-domain.replit.dev

# Session
SESSION_SECRET=dev-secret-change-in-production

# Error Tracking (Optional - Sentry)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Analytics (Optional - Google Analytics, disabled in development by default)
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Frontend Error Tracking (Optional - Sentry)
# VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Regional Configuration
DEFAULT_COUNTRY=HK
DEFAULT_COUNTRY_CODE=+852
DEFAULT_LANGUAGE=zh-HK

# Development Notes:
# - Rate limiting is disabled in development
# - Analytics are disabled by default in development
# - Session cookies are not secure (HTTP allowed)
