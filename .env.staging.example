# Staging Environment Configuration
# This file shows recommended settings for staging environment

# Application Environment
NODE_ENV=staging

# Server Configuration
PORT=5000

# Database (Required - Use staging database)
DATABASE_URL=postgresql://user:password@staging-host:port/database

# Authentication (Required - Use staging domain)
REPLIT_DOMAINS=your-app-staging.replit.dev

# Session (Required - Use secure, random session secret)
SESSION_SECRET=staging-session-secret-change-this

# Error Tracking (Recommended - Sentry)
SENTRY_DSN=https://your-staging-sentry-dsn@sentry.io/project-id

# Analytics (Optional - Use staging GA property)
VITE_GA_MEASUREMENT_ID=G-STAGING-ID

# Frontend Error Tracking (Recommended - Sentry)
VITE_SENTRY_DSN=https://your-staging-sentry-dsn@sentry.io/project-id

# Staging-specific settings:
# - Session cookies: Secure (HTTPS only)
# - Rate limiting: Enabled but lenient
# - Sentry trace sample rate: 100%
# - Analytics: Enabled for testing
