# Production Environment Configuration
# This file shows recommended settings for production environment

# Application Environment
NODE_ENV=production

# Server Configuration
PORT=5000

# Database (Required - Use production database)
DATABASE_URL=postgresql://user:password@production-host:port/database

# Authentication (Required - Use production domain)
REPLIT_DOMAINS=your-app.replit.app

# Session (Required - Use strong, cryptographically random session secret)
SESSION_SECRET=production-session-secret-use-strong-random-value

# Error Tracking (Highly Recommended - Sentry)
SENTRY_DSN=https://your-production-sentry-dsn@sentry.io/project-id

# Analytics (Recommended - Google Analytics)
VITE_GA_MEASUREMENT_ID=G-PRODUCTION-ID

# Frontend Error Tracking (Highly Recommended - Sentry)
VITE_SENTRY_DSN=https://your-production-sentry-dsn@sentry.io/project-id

# Production-specific settings:
# - Session cookies: Secure (HTTPS only)
# - Session max age: 7 days
# - Rate limiting: Strict (100 req/15min)
# - Sentry trace sample rate: 10%
# - Analytics: Enabled
# - Logging level: warn
